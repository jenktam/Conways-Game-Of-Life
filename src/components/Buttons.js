import React from 'react'

const Buttons = props => {
  return (
    <div>
      <button>Clear</button>
      <button>Randomize</button>
      <button>Play/Pause</button>
      <button>Step</button>
    </div>
  )
}

export default Buttons
